import { AppShell } from '@/components/app-shell';

export default function PermitsPage() {
  return (
    <AppShell title="Permits">
      <p className="text-sm text-slate-600">Permits module scaffold (see plan.md).</p>
    </AppShell>
  );
}
