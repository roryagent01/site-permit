import { AppShell } from '@/components/app-shell';

export default function RemindersPage() {
  return (
    <AppShell title="Reminders">
      <p className="text-sm text-slate-600">Reminders module scaffold (see plan.md).</p>
    </AppShell>
  );
}
