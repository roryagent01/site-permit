import { AppShell } from '@/components/app-shell';

export default function QualificationsPage() {
  return (
    <AppShell title="Qualifications">
      <p className="text-sm text-slate-600">Qualifications module scaffold (see plan.md).</p>
    </AppShell>
  );
}
