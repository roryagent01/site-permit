import { AppShell } from '@/components/app-shell';

export default function DashboardPage() {
  return (
    <AppShell title="Dashboard">
      <p className="text-sm text-slate-600">Dashboard module scaffold (see plan.md).</p>
    </AppShell>
  );
}
