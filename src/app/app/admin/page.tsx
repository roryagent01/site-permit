import { AppShell } from '@/components/app-shell';

export default function AdminPage() {
  return (
    <AppShell title="Admin">
      <p className="text-sm text-slate-600">Admin module scaffold (see plan.md).</p>
    </AppShell>
  );
}
